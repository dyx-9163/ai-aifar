- ASAR 与外层目录校验分离为 validateAsarInventory / validateOuterInventory 两个独立函数，分别维护 allowed/forbidden 清单。
- 路径统一通过 normalizeInventoryPath 将反斜杠替换为正斜杠并去除前导斜杠，确保跨平台一致性。
- 文档文件采用 YYYY-MM-DD 日期前缀命名，并按 plans/specs/reports 三类用途分目录存放。