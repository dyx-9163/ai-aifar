首次使用前需执行 `Copy-Item .env.example .env` 生成 `.env`；通过 `docker compose --env-file .env --profile {cpu|hybrid|gpu} up -d` 启动，`down` 停止；Windows 下可使用 `./start-model.ps1 [gpu|hybrid|cpu]`、`./status-model.ps1`、`./stop-model.ps1`、`./verify-model.ps1` 管理生命周期。GPU profile 要求宿主机启用 Docker GPU 支持。