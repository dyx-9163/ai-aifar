- 每个 Electron 入口（main、preload、agent worker）对应独立的 vite.*.config.ts 配置文件，由 forge.config.ts 统一注册构建任务。
- TypeScript 类型检查拆分为 tsconfig.json、tsconfig.node.json、tsconfig.type-tests.json 三套配置，分别用于源码、Node 端和 Vitest 测试。
- 通过 pnpm overrides 锁定 @electron/rebuild 到 4.x，规避私有网络下 Git 依赖不可达问题。